# == Schema Information
#
# Table name: tickets
#
#  id               :integer          not null, primary key
#  assigned_to_type :string           not null
#  assigned_to_id   :integer          not null
#  created_at       :datetime         not null
#  updated_at       :datetime         not null
#

one:
  assigned_to: one
  assigned_to_type: Assigned to

two:
  assigned_to: two
  assigned_to_type: Assigned to
